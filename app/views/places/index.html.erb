

<% @user = User.find_by({ "id" => session["user_id"] }) %>
<% if @user != nil %>
<h2 >Places</h2>

<div class="mb-3">
  <a href="/places/new" class="btn btn-outline-secondary">New place</a>
</div>

<div class="mb-3">
  <ul class="list-group">
    <% for place in @places %>
      <li class="list-group-item"><a href="/places/<%= place["id"] %>"><%= place["name"] %></a></li>
    <% end %>
  </ul>
</div>

<% else %>
<h2>You Don't Have Access</h2>
  You must first <a href="/login">login</a> to view your places.
<% end %>